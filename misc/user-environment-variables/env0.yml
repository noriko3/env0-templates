version: 1

variables:
  - key:
      "USER_NAME"
    value:
      "user"
  - key:
      "USER_EMAIL"
    value:
      "user@domain.com"


deploy:
  steps:
    terraformPlan:
      before:
        - echo "user name"
        - echo $USER_NAME
        - echo "user email"
        - echo $USER_EMAIL
