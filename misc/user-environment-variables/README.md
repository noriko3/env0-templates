# Env0 System Environment Variables

This folder will deploy `null-resource` environment to `env0` <br> 
and will also `echo` user defined environment variables that were defined in the env0.yml file <br>
